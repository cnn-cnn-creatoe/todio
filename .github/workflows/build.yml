name: Build SoftDo

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build app
        run: npm run build

      - name: Build Windows installer
        run: npx electron-builder --win --publish never

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SoftDo-Windows
          path: |
            dist-electron/*.exe
            dist-electron/*.exe.blockmap

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build app
        run: npm run build

      - name: Build macOS installer
        run: npx electron-builder --mac --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SoftDo-macOS
          path: |
            dist-electron/*.dmg
            dist-electron/*.zip

  create-release:
    needs: [build-windows, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: SoftDo-Windows
          path: release-files

      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: SoftDo-macOS
          path: release-files

      - name: List files
        run: ls -la release-files/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.1.0
          name: SoftDo v1.1.0
          body: |
            ## ✨ SoftDo v1.1.0

            A beautiful desktop todo widget with glassmorphism design.
            精美的桌面待办小组件，采用玻璃拟态设计。

            ### 🆕 What's New | 新功能

            - 🔧 **Fixed Shadow Issue** - Clean transparent window on all backgrounds
            - 🖥️ **System Tray Support** - Minimize to tray, quick access menu
            - 🔄 **Auto Update Check** - Checks for new versions from GitHub
            - 📌 **Desktop Shortcut** - Automatically created during installation

            ### 📥 Downloads | 下载

            | Platform | File |
            |----------|------|
            | Windows | `SoftDo-1.1.0-Windows.exe` |
            | macOS | `SoftDo-1.1.0-macOS.dmg` |

            ### ✨ Features | 功能
            - 🎨 Premium glassmorphism design | 高级玻璃拟态设计
            - ⏰ Smart due times with countdown | 智能截止时间与倒计时
            - 💾 Auto-save to local storage | 本地自动保存
            - 🖱️ Draggable desktop widget | 可拖动桌面小组件
            - 📌 Always-on-top option | 窗口置顶选项
            - 🔔 System tray with tooltip | 系统托盘与提示
          files: release-files/*
          draft: false
          prerelease: false
